<Window x:Class="AutoPilotAgent.UI.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Action" Height="360" Width="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="This action requires confirmation:" FontWeight="SemiBold" />

        <StackPanel Grid.Row="1" Margin="0,10,0,10">
            <TextBlock Text="Action Type:" FontWeight="SemiBold" />
            <TextBlock Text="{Binding ActionType}" />

            <TextBlock Margin="0,10,0,0" Text="Expected Result:" FontWeight="SemiBold" />
            <TextBlock Text="{Binding ExpectedResult}" TextWrapping="Wrap" />

            <TextBlock Margin="0,10,0,0" Text="Parameters:" FontWeight="SemiBold" />
        </StackPanel>

        <TextBox Grid.Row="2" Text="{Binding ParametersJson, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="90" Margin="0,0,8,0" Content="Cancel" IsCancel="True" Click="Cancel_Click" />
            <Button Width="90" Content="Approve" IsDefault="True" Click="Approve_Click" />
        </StackPanel>
    </Grid>
</Window>
